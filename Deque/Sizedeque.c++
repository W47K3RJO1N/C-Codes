#include <bits/stdc++.h>
using namespace std;

int main() {
    deque<int> d;
    d.push_back(1);
 //   d.push_back(3);
  //  d.push_front(2);
    
  //  d.pop_back();
    
    for (int val : d) { 
     //   cout << val << " ";//
        cout << "The size of the deque is: " << d.size();
    }
        return 0;
}
